import { Button } from "@/components/ui/button.tsx";
import {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuItem,
} from "@/components/ui/dropdown-menu.tsx";

type Props = {
  onEdit: () => void;
};

export default function ActuariesDropdownMenu({ onEdit }: Props) {
  return; //logika za tipove
}
